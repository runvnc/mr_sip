# Core dependencies
baresipy>=0.1.0
scipy>=1.11.0
numpy>=1.24.0
JACK-Client>=0.5.4

# PySIP for S2S mode (replaces baresip/JACK for S2S)
PySIPio @ git+https://github.com/runvnc/PySIP.git

# New dependencies for STT provider interface
inotify>=0.2.10
websockets>=12.0
deepgram-sdk>=3.0.0

# Optional: for development/testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Optional local STT provider dependencies
#
# The default Deepgram providers do NOT need these packages.  Do not install
# them on lightweight/incoming-only Deepgram deployments; onnxruntime-gpu and
# the Silero/Torch stack can pull large CUDA/NVIDIA wheels.
#
# Install only when using STT_PROVIDER=silero_cohere:
#   pip install -e ".[silero-cohere]"
#
# Install only when using STT_PROVIDER=smart_turn_v3:
#   pip install -e ".[smart-turn-v3]"
#
# Convenience extra for all local STT providers:
#   pip install -e ".[local-stt]"
